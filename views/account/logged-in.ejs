<%# Content Heading %>
<% if (title) { %>
    <h1><%= title %></h1>
<% } else {
    res.redirect('/')
} %>

<%- messages() %>

<% if (errors) { %>
    <ul class="notice">
        <% errors.array().forEach(error => { %>
            <li><%= error.msg %></li>
        <%  }) %>
   </ul>
<% } %>

<h2>Welcome <%= user.account_firstname %></h2>
<p><a href="/account/update-account/<%= user.account_id%>">Update Account information</a></p>
<% if(user.account_type != 'Client') { %>
    <h3>Inventory Management</h3>
    <p><a href="/inv">Link</a></p>
<% } %>